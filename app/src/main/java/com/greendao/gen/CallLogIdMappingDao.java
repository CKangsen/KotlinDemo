package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CALL_LOG_ID_MAPPING".
*/
public class CallLogIdMappingDao extends AbstractDao<CallLogIdMapping, Long> {

    public static final String TABLENAME = "CALL_LOG_ID_MAPPING";

    /**
     * Properties of entity CallLogIdMapping.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Calllog_id = new Property(1, Integer.class, "calllog_id", false, "CALLLOG_ID");
        public final static Property Md5 = new Property(2, String.class, "md5", false, "MD5");
        public final static Property Openid = new Property(3, String.class, "openid", false, "OPENID");
    }


    public CallLogIdMappingDao(DaoConfig config) {
        super(config);
    }
    
    public CallLogIdMappingDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CALL_LOG_ID_MAPPING\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"CALLLOG_ID\" INTEGER NOT NULL ," + // 1: calllog_id
                "\"MD5\" TEXT NOT NULL UNIQUE ," + // 2: md5
                "\"OPENID\" TEXT NOT NULL );"); // 3: openid
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CALL_LOG_ID_MAPPING\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CallLogIdMapping entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getCalllog_id());
        stmt.bindString(3, entity.getMd5());
        stmt.bindString(4, entity.getOpenid());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CallLogIdMapping entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getCalllog_id());
        stmt.bindString(3, entity.getMd5());
        stmt.bindString(4, entity.getOpenid());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CallLogIdMapping readEntity(Cursor cursor, int offset) {
        CallLogIdMapping entity = new CallLogIdMapping( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // calllog_id
            cursor.getString(offset + 2), // md5
            cursor.getString(offset + 3) // openid
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CallLogIdMapping entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCalllog_id(cursor.getInt(offset + 1));
        entity.setMd5(cursor.getString(offset + 2));
        entity.setOpenid(cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CallLogIdMapping entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CallLogIdMapping entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CallLogIdMapping entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
