package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOCAL_UPDATE_CONFLICT_CONTACT".
*/
public class LocalUpdateConflictContactDao extends AbstractDao<LocalUpdateConflictContact, Long> {

    public static final String TABLENAME = "LOCAL_UPDATE_CONFLICT_CONTACT";

    /**
     * Properties of entity LocalUpdateConflictContact.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Contact_id = new Property(1, Integer.class, "contact_id", false, "CONTACT_ID");
        public final static Property Openid = new Property(2, String.class, "openid", false, "OPENID");
    }


    public LocalUpdateConflictContactDao(DaoConfig config) {
        super(config);
    }
    
    public LocalUpdateConflictContactDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOCAL_UPDATE_CONFLICT_CONTACT\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"CONTACT_ID\" INTEGER NOT NULL UNIQUE ," + // 1: contact_id
                "\"OPENID\" TEXT NOT NULL );"); // 2: openid
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOCAL_UPDATE_CONFLICT_CONTACT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LocalUpdateConflictContact entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getContact_id());
        stmt.bindString(3, entity.getOpenid());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LocalUpdateConflictContact entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getContact_id());
        stmt.bindString(3, entity.getOpenid());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LocalUpdateConflictContact readEntity(Cursor cursor, int offset) {
        LocalUpdateConflictContact entity = new LocalUpdateConflictContact( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // contact_id
            cursor.getString(offset + 2) // openid
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LocalUpdateConflictContact entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setContact_id(cursor.getInt(offset + 1));
        entity.setOpenid(cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LocalUpdateConflictContact entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LocalUpdateConflictContact entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LocalUpdateConflictContact entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
